/**
 * Administrative and maintenance tools.
 * <p>
 * Each class in this package should have its own {@code main()} method
 * for standalone execution (e.g. database migrations, data seeding,
 * admin scripts).
 * </p>
 */
package ${package}.tools;
